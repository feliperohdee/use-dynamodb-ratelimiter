{
	"name": "use-dynamodb-ratelimiter",
	"devDependencies": {
		"@aws-sdk/client-dynamodb": "^3.868.0",
		"@aws-sdk/lib-dynamodb": "^3.868.0",
		"@types/node": "^22.10.10",
		"@vitest/coverage-v8": "^3.2.4",
		"eslint": "^9.26.0",
		"lodash": "^4.17.21",
		"prettier": "^3.4.2",
		"typescript": "^5.7.3",
		"typescript-eslint": "^8.32.0",
		"use-async-helpers": "^1.0.31",
		"use-dynamodb": "^1.0.72",
		"vitest": "^3.2.4",
		"zod": "^4.0.17"
	},
	"peerDependencies": {
		"@aws-sdk/client-dynamodb": "^3.868.0",
		"@aws-sdk/lib-dynamodb": "^3.868.0",
		"lodash": "^4.17.21",
		"use-async-helpers": "^1.0.31",
		"use-dynamodb": "^1.0.72",
		"zod": "^4.0.17"
	},
	"files": [
		"dist"
	],
	"license": "MIT",
	"main": "dist/index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/feliperohdee/use-dynamodb-ratelimiter"
	},
	"scripts": {
		"build": "rm -rf dist && yarn lint && tsc -p tsconfig.json",
		"dev:dynamodb": "java -jar /Users/feliperohde/Documents/dynamodb-local/DynamoDBLocal.jar -dbPath ./dynamodb-local -sharedDb",
		"lint": "prettier --write . && eslint --fix . && tsc -p tsconfig.json",
		"npm:publish": "yarn test && yarn build && yarn version --patch --no-git-tag-version && yarn publish --non-interactive",
		"test": "yarn vitest --fileParallelism=false --run",
		"test:coverage": "rm -rf coverage && yarn vitest --fileParallelism=false --coverage --run",
		"test:watch": "yarn vitest --fileParallelism=false"
	},
	"version": "1.0.9"
}
